spring.application.name=agendamento

spring.jpa.show-sql=${APP_DATABASE_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create

spring.datasource.url=${db_url_agendamento:jdbc:postgresql://localhost:5432/agendamento}
spring.datasource.username=${agendamento_username:postgres}
spring.datasource.password=${agendamento_password:Admin123#}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do servidor
server.port=${SERVER_PORT:8080}

# Configurações do envio de email
spring.mail.host=localhost
spring.mail.port=1027
spring.mail.username=
spring.mail.password=

spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false


jwt.secret=Kj@t07$.256.bit.secret87hdaçobh@

server.connection-timeout=-1
nominatim.api.url=https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat={latitude}&lon={longitude}

spring.activemq.broker-url=tcp://localhost:61616
spring.activemq.user=admin
spring.activemq.password=admin
spring.jms.pub-sub-domain=false